# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Rate limiter library. Java 21 project using IntelliJ IDEA.

## Build System

Gradle with Groovy DSL. Java 21 toolchain.

- **Build**: `./gradlew build`
- **Test**: `./gradlew test`
- **Run**: `./gradlew run`
- **Clean**: `./gradlew clean`
